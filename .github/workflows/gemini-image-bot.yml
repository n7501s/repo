name: Gemini Image Bot

on:
  issues:
    types: [opened]
  issue_comment:
    types: [created]

jobs:
  chat:
    # Задейства се САМО ако заглавието на Issue-то започва с думичката "[TEST]"
    # Това пази всички останали твои разговори напълно чисти от този експеримент!
    if: github.event.sender.type != 'Bot' && contains(github.event.issue.title, '[TEST]')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install Dependencies
        run: npm install @octokit/action @google/genai

      - name: Run Gemini Image Bot
        uses: actions/github-script@v7
        env:
          GEMINI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        with:
          script: |
            const { GoogleGenAI } = require('@google/genai');
            
            if (!process.env.GEMINI_API_KEY) {
              throw new Error("Грешка: Не е намерен GEMINI_API_KEY в GitHub Secrets!");
            }
            
            const ai = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY });
            
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const issue_number = context.issue.number;

            const issue = await github.rest.issues.get({ owner, repo, issue_number });
            const comments = await github.rest.issues.listComments({ owner, repo, issue_number });

            const contents = [];

            // Добавяме първото съобщение
            const firstParts = [{ text: `Контекст: заглавието на чата е "${issue.data.title}". Първо съобщение: ${issue.data.body || "Здравей"}` }];
            contents.push({ role: "user", parts: firstParts });

            // Обхождаме коментарите и проверяваме за снимки
            for (const comment of comments.data) {
              const role = comment.user.type === 'Bot' ? 'model' : 'user';
              const commentParts = [{ text: comment.body || "" }];

              // Търсим линкове към качени в GitHub картинки в текна на коментара
              const imageUrls = comment.body.match(/https:\/\/(?:github\.com\/user-attachments\/assets\/|github\.com\/user-attachments\/assets\/|[a-zA-Z0-9-]+\.githubusercontent\.com\/[^\s)]+)/g);

              if (imageUrls && role === 'user') {
                for (const imgUrl of imageUrls) {
                  try {
                    const imgResponse = await fetch(imgUrl);
                    const imgBuffer = await imgResponse.arrayBuffer();
                    const base64Data = Buffer.from(imgBuffer).toString('base64');
                    
                    commentParts.push({
                      inlineData: {
                        data: base64Data,
                        mimeType: "image/jpeg"
                      }
                    });
                  } catch (imgErr) {
                    console.error("Неуспешно изтегляне на снимка:", imgErr);
                  }
                }
              }

              contents.push({ role: role, parts: commentParts });
            }

            try {
              const response = await ai.models.generateContent({
                model: 'gemini-3.5-flash-lite',
                contents: contents,
              });

              const botResponse = response.text;

              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number,
                body: botResponse
              });
            } catch (error) {
              console.error("Грешка при комуникация с Gemini (Снимки):", error);
              throw error;
            }
